import { api } from "../../../api";

// This file serves as a catch-all route for the Next.js API,
// handling all API requests by routing them through Remult.

export const { POST, PUT, DELETE, GET } = api;
