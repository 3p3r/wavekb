import { Query, Upload } from "./Embeddable";
import { Embedding, Signal } from "./Embedding";
import { Account, Session, User, Verification } from "./Auth";
import { CrossCorrelation, Spectrogram, Project } from "./Project";

export const entities = [
  Query,
  Upload,
  Account,
  Session,
  User,
  Verification,
  Embedding,
  Signal,
  CrossCorrelation,
  Spectrogram,
  Project,
];
